[project]
name = "synapse"
version = "0.1.0"
description = "Synapse: Cross-platform multi-process Python daemon for local process telemetry"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=0.10.0",
    "uvloop>=0.22.1; sys_platform != 'win32'",
    "psutil",
    "sqlglot>=30.12.0",
]

[project.optional-dependencies]
linux = [
    "uvloop>=0.19.0; sys_platform != 'win32'"
]
windows-macos = []
build = [
    "nuitka>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/daemon", "src/domain", "src/ipc", "src/telemetry", "src/cli", "src/transpiler"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "mypy>=1.8.0",
    "ruff>=0.2.0",
    "nuitka>=2.0.0",
    "hypothesis>=6.156.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM", "ARG"]
ignore = ["E501", "ARG001", "ARG002", "ARG005", "B007", "N801", "B905", "SIM105", "SIM115"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
